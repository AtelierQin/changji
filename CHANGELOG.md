# Changelog

所有对长技 · 凡人天梯的可见改动都记录在这里。版本遵循 [语义化版本](https://semver.org/)。

页面内也有一份结构化的更新日志（方法 tab 底部 + topbar「日志」按钮），由 `CHANGELOG_ENTRIES` 数组渲染。修改本文件时请同步更新 JS 数组。

## [3.0.1] — 2025-XX-XX

### 修改

- **方法 tab 引用直链原文**：5 个引用链接从 `REFERENCES.md#N` 改为论文原文 URL。
  - Dreyfus 1980 → Open Library `Mind over Machine`
  - Lally 2010 → Wiley（DOI 10.1002/ejsp.674）
  - Ericsson 1993 → APA PsycNet（DOI 10.1037/0033-295X.100.3.363）
  - Salomon & Perkins 1989 → SAGE（DOI 10.3102/0013189X018009042）
  - Flavell 1979 → APA PsycNet（DOI 10.1037/0003-066X.34.10.906）
- 文案从「详见引用 →」改为「论文原文 →」，点击跳转新标签页（target="_blank" rel="noopener"）。
- **REFERENCES.md 同步**：每节加上"原文"行，DOI 字段改为可点击。

## [3.0.0] — 2025-XX-XX

重大重设计：从 narrative-style 长滚动改为 **底部 5 tab 单页**。

### 修改

- **HTML 结构**：从 9 节长滚动 → 5 个 tab pane（角色 / 树 / 今日 / 日志 / 方法），每次只显示一个。
- **顶部精简到一行**：品牌 + 角色 pill（点击切到角色 tab）+ 日志 / 设置两个图标按钮。
- **底部固定 tab bar**：5 个 tab 均分宽度，激活态有顶部 border + 加粗。
- **URL hash 路由**：`#character` / `#tree` / `#today` / `#log` / `#method`，浏览器前进后退工作。
- **默认 tab = `树`**：daily-use 最常见的探索场景。
- **设置集中化**：导出 / 导入 / 重置 / 关于 README 全部进「设置」弹窗（之前散落在 footer）。
- **更新日志双位置**：方法 tab 底部展示 + topbar「日志」按钮触发完整弹窗。

### 新增

- **角色 pill（topbar 中央）**：实时显示 `Lv.X · 第 N 天`，点击切到角色 tab。
- **今日 tab 顶部 summary**：今日已获 XP / 笔触状态 / 周节律三项 inline。
- **日志 tab 顶部统计**：总记录 / 近 7 日 / 升级次数 / 累计 XP 四张卡。
- **fadeIn 动画**：tab 切换时新 pane 0.2s 淡入。

### 设计依据

每次来只做一件事：勾 / 看 / 记 / 回顾。把 mental mode 拆成独立屏，专注度提升，"滚到底才能看到方法"的疲惫消失。

### 文件变化

- 主文件：`real-life-skill-tree.html`（v3.0，tabbed，2126 行）
- 备份：`real-life-skill-tree-v1.1.3-narrative.html`（v1.1.3 长滚动版，2164 行）
- 数据 schema 不变（仍是 `changji-state` v1）；旧 v1.1.x / v1.0.x 用户数据自动兼容。

## [1.1.3] — 2025-XX-XX

### 新增

- **更新日志 tab** —— topnav 加 `<a href="#changelog">更新日志</a>` 链接；页面末尾新增"第九卷 · 版本"章节，渲染 `CHANGELOG_ENTRIES` 数组。
- **结构化展示** —— 每条改动带类型徽章（新增 / 改动 / 修复 / 设计 / 备注），比本文件更易扫读。
- **JS 数据镜像本文件** —— `CHANGELOG_ENTRIES` 数组与本文件语义一致，修改时两边同步。

## [1.1.2] — 2025-XX-XX

13 项技能的修习内容从占位符补到具体条目。原 7 项不动。

### 新增

- **身**：力量（复合动作 / 推拉腿分化 / 自重循环）、柔韧（静态拉伸 / 关节绕环 / 阴瑜伽）、耐力（长距慢跑 / 通勤骑车 / 楼梯）
- **心**：记忆（Anki / 复述要点 / 关键词记名）、逻辑（标前提-结论-假设 / 反驳自己 / 苏格拉底追问）
- **技**：沟通（即兴介绍 / 复述再表态 / 三抽象层级讲同一事）、学习（教别人 / 认知地图 / 间隔复习）、决策（列最大损失 / 10-10-10 / 可逆小实验）
- **群**：边界（拒绝一个请求 / 边界清单 / 练说"我不"）、关系（联系老友 / 主动约见 / 写信）、领导（无议程讨论 / 放手给任务 / 复盘失败）
- **道**：价值（动机复盘 / 不妥协清单 / 冲突暂停 24h）、意义（值得瞬间 / 30 天目标 / 回到核心问题）

### 设计原则（每项 3 条统一遵守）

1. **单次可完成**：最长 90 分钟，最短 5 分钟
2. **执行 + 反思混合**：3 条里通常 2 条执行类 + 1 条反思类
3. **不重叠**：与已有技能条目不重复
4. **无依赖**：不假设有器械 / 伙伴 / 付费工具

### 不变

- 7 项已有技能（body-1 / mind-1 / mind-4 / skill-2 / social-1 / spirit-1 / spirit-2）的内容保留
- 渲染逻辑、fallback 占位符（防御性保留）
- 升级 / 重置 / 导出 / 导入

### 未来（v2 范围，未做）

- **任务类型换挡**（契克森米哈赖）：同一技能在 Lv.4 时条目侧重"教别人 / 跨场景"，在 Lv.5 侧重"风格化"。当前条目是 Lv.0 → Lv.3 的入门到熟手级别。

## [1.1.1] — 2025-XX-XX

按圆桌第二轮（Sivers + 7 位共识）落地：废除 streak 显示，改为周节律。

### 修改

- **角色面板"连修"卡片 → "周节律"** —— 不再显示"连续 N 天"。改为显示过去 7 天中有活动的天数（N/7）。
  - 旧："连修 23 天"  + "再 7 天进入自动化区间"
  - 新："周节律 4 / 7 天" + "本周围 4 天，节奏起来了。"
- **`computeStreak` → `computeWeeklyCadence`** —— 算法从"连续天数"改为"过去 7 天有活动的天数"。Loss aversion 移除。
- **同步修复盲点 7** —— 3 天高强度 + 4 天休息的人不再被惩罚为"断了"。他们现在显示"周节律 3 / 7"，与连续 7 天的人同为稳定参与者（一个连续型、一个集中型）。
- **数据来源合并** —— 周节律同时统计 `STATE.log`（quest/practice/levelup 事件）和 `STATE.dailyBrush`（自画笔触）。两类活动都算"今天有做过"。

### 设计依据

- Sivers："streak-as-display = loss aversion = burnout"
- 克利尔 + Jobs：身份由"我做了"涌现，不需要"我连续了多少天"加固
- 契克森米哈赖 + 爱泼斯坦：保留信号（频率存在），替换焦虑源（连续天数消失）
- 8 人一致同意废除 streak-as-display，本轮唯一争议在用什么替代——最终采纳"周节律 N/7"

### 不变

- 角色面板其他三张卡片（总经验 / 已解锁 / 已登顶）
- 详情面板、Quests、今日一笔、Featured、Log
- 导出 / 导入 schema（旧 v1.0 数据自动兼容，无迁移步骤）

## [1.1.0] — 2025-XX-XX

按圆桌讨论（7 人 4 轮）共识所做的 v1 阶段优化 + 部分 v2 补丁前移。

### 新增

- **「今日一笔」字段**（克利尔"身份自画"补丁，前移自 v2）—— 每天一句话"今天我做了一次 X 会做的事"，输入即保存，1 步操作。200 天后笔触累积成画。这是克利尔补丁的最小可用落地。
- **定性化挑战显示**（克利尔 + 契克森米哈赖共识）—— 角色面板和技能详情页的"下一关"现在显示**下一级挑战的文字描述**（如"在熟悉场景下能稳定执行"），不再是裸数字"420 / 800 XP"。XP 数字保留在数据模型中（纽波特工程地基），但不再突出到 UI 前景。
- **等级挑战表 `LEVEL_CHALLENGES`** —— 五个等级过渡各有挑战描述 + "本周可尝试"具体动作。契克森米哈赖的"挑战类型换挡"在这里落到了文字层，不需 UI 单独按钮。
- **笔触累计计数** —— 顶部"累计 N 笔"指示器，让连续记录有可视反馈但不成为外部激励。
- **章节新增"第五卷半 · 今日一笔"** —— 在 Quests 之前；叙事顺序调整为：观察（hero）→ 角色 → 树 → 亮技 → 方法 → 一笔（身份自画）→ 今日修习（做了什么）→ 日志（既往痕迹）→ 启程。
- **章节编号前移** —— 今日修习从"第五卷"改"第六卷"，日志从"第六卷"改"第七卷"，CTA 从"第七卷"改"第八卷"。
- **`scaffold-note` 视觉标记** —— "v1 脚手架"小标识，提醒用户当前是入门脚手架而非终态（与纽波特三阶段方案一致）。
- **schema 兼容旧用户** —— `loadState()` 对旧 v1 数据自动补 `dailyBrush: {}`，无需用户迁移操作。

### 修改

- **角色面板** —— XP 行不再写"/2500"，改为单数字 + 定性挑战描述 + 进度条；"已解锁"、"已登顶"、"连修"四个指标各自的"下一步"提示文字化。
- **详情面板** —— "距下一级 N/M XP"行上面新增"Lv.N → Lv.N+1 挑战"主标题；保留底部小字 N/M 数字作为参考。
- **章节顺序调整** —— brush 在 quests 前（先决定"我是谁"，再记录"我做了什么"）。

### 不变

- 20 项封闭树、5 个固定标签、Quests 列表、导出/导入、reset —— v1 脚手架整体保留，与纽波特"v1 脚手架 / v2 90 天后开放 / v3 完全自定义"三阶段方案一致。

## [1.0.0] — 2025-XX-XX

首个正式发布版。从 `v0.4`（topnav 标识）直接升到 `1.0.0`，因为这一版是一次完整的工程重构。

### 新增

- **真实持久化**：所有 XP / 等级 / quest 进度存到带 schema 版本的 `localStorage`，跨刷新 / 跨标签存活
- **动态角色面板**：总 XP、解锁数、登顶数、连修天数从状态派生，不再是硬编码假数字
- **动态日志**：最近修习从实际活动派生，"查看全部日志" 弹出完整列表
- **连修 (streak) 计算**：按"是否有任何活动"按日聚合
- **导出 / 导入**：JSON 格式，跨设备迁移、防数据丢失
- **前置强制**：`mind-4` 必须逻辑 Lv.3 + 记忆 Lv.2 才能升级，等等
- **重置级联**：重置技能时，相关 quest 也回到未完成
- **REPL 接口**：浏览器 Console 可调用 `CJ.export()` / `CJ.import(json)` / `CJ.reset()` / `CJ.state()`
- **`<noscript>` 回退**：JS 关闭时仍能看到所有内容
- **meta description / og / twitter card**：分享时显示预览
- **README / LICENSE / CHANGELOG / REFERENCES / REFLECTION**：项目根基础文件
- **参考文献文档**：[REFERENCES.md](./REFERENCES.md) — 方法论每条引用都可追溯
- **7 日反思模板**：[REFLECTION.md](./REFLECTION.md) — 页脚承诺的内容终于有了

### 重构

- **单一数据源**：之前技能等级/XP 同时写在 SVG markup 和 JS 对象，现在 SVG 完全由数据生成
- **统一事件绑定**：移除所有内联 `onclick` / `onkeydown`，统一 `addEventListener` + 事件委托
- **统一状态机**：`STATE` 对象是唯一可变状态；每次变更后 `persist()` 一次
- **移除 magic number**：动画时长、XP 阈值、跨级公式都命名 / 提取成常量
- **可访问性**：SVG 节点带 `aria-label`，原生 `<input type="checkbox">` 替代 div 模拟，焦点环在所有交互元素可见
- **`color-mix()` 提供 fallback**
- **`try/catch` 包裹 init**：单个错误不再让页面半渲染

### 修复

- 旧版 `resetSkill` 改数据但不刷新 SVG 节点 — 现在通过同一 update 路径级联
- 旧版 `checkLevelUp` 原地改写 `s.xpNext` — 现在 `xpInLevel` / `xpRequired` 显式
- 旧版页脚全部 `href="#"` — 现在指向真实文件或显式 `aria-disabled`
- 旧版"含 7 日反思模板"营销承诺落空 — 现在真有 `REFLECTION.md`

### 安全 / 隐私

- 所有渲染前对字符串做 HTML 转义（修复潜在 XSS）
- 不再内联 onclick 字符串插值 ID（修复模板注入风险）
- 数据全本地，无第三方分析

---

## [0.4.0] — 2025-XX-XX

最后一次 demo 版本。

- 完整视觉设计
- 五脉二十技静态 SVG
- 角色面板、日志、quest 列表（全部硬编码示例数据）
- 选中节点保存到 localStorage
- 升阶 / 重置 / 记录修习 基础交互

## [0.1.0 – 0.3.0] — 2025

早期探索。骨架、token、单分支试做。
