# Changelog

所有对长技 · 凡人天梯的可见改动都记录在这里。版本遵循 [语义化版本](https://semver.org/)。

页面内也有一份结构化的更新日志（方法 tab 底部 + topbar「日志」按钮），由 `CHANGELOG_ENTRIES` 数组渲染。修改本文件时请同步更新 JS 数组。

---

## [1.3.0] — 2026-09-07

### 设计

- **去掉前置系统** — 所有 20 项技能可直接练习，不再被"必须先解锁 X 才能练 Y"卡住。
  - 删除 `PREREQS` 常量、`prereqMet()` / `prereqList()` 实际逻辑（保留空壳为 no-op 兼容）
  - `pickDailyQuests` 不再过滤未达成前置的 quest
  - `toggleQuest` / `logPractice` 去掉前置检查
  - 详情面板去掉「前置 · 未达成」段；记录按钮不再因前置被禁用
  - 今日 quest 列表去掉「（前置未达成）」标记和禁用样式
  - Lv.0 描述从「尚未开始，前置或意愿未到」改为「尚未开始，意愿未到」
- **理由**：原设计假设"先练基础再练高阶"是用户需求，但实际是给探索设了硬性门控 — 用户想练"创造"也得先练"逻辑 Lv.3"+"记忆 Lv.2"，反而把"创造"的兴奋感延后到几个月后。"先练基础"的引导交给协同线 + 推荐修习的自然展示，不再硬性门控。

---

## [1.2.8] — 2026-09-07

### 新增

- **Quest 系统**：硬编码 5 个 → 24 候选池；按 dayKey 动态选 5 个；自动判定周挑战（5 种）+ 完成发放 XP。
- **数据迁移**：`SCHEMA_MIGRATIONS` 机制 + `loadState` 尽力兼容任意 version；备份提醒每 14 天。
- **导入导出升级**：下载 .json 文件 + 复制 base64 分享链接 + 文件选择/拖放/粘贴 多种方式。
- **今日一笔增强**：tag 系统（空格分隔）+ 90 天热力图（45 列紧凑布局）；brush 数据 shape 升级为 `{ text, tags }`。
- **Topbar 重设计**：长技 + 每日名人名言；Wikiquote JSONP 拉取 + prev/next 按钮手动切换 + 32 条本地精选 fallback。

### 设计

- 布局重构：底部 tabbar → 左侧 sidebar（220px sticky，分组：旅程 / 每日 / 参考）；topbar 元动作下沉到 sidebar meta 区。
- max-width 1200 → 1600 充分利用屏幕宽度。
- Topbar 镜像 `.app-layout` grid：长技对齐 sidebar 内容，名言对齐 main 内容。
- Topbar quote 支持最多两行（`-webkit-line-clamp: 2`），长句完整可读。
- 笔触热力图 15 列 → 45 列（GitHub 贡献图风格），90 天 = 2 行。

### 安全

- **分享链接二次确认**：本地有数据时弹 confirm，避免误点覆盖进度。
- **OAuth 改用 code 交换**：回调只返回 60s TTL 一次性 code，前端 POST 换 JWT，避免 30 天 JWT 留在浏览器历史。
- **OAuth state 校验**：用 HttpOnly cookie 防 CSRF。
- **Worker 校验**：`/sync PUT` 加 Content-Length + 字段类型 + 序列化后大小（256KB）多重限制。

### 修复

- **XP 算法 bug**：`xpRequiredForLevel(0)` 返回 0 导致任意勾选 quest 立刻 Lv.0→1 升阶；改为返回累计阈值差（100/200/500/1200/3000）。
- **maxXP 计算**：20 项技能累加的 totalXP 配 5100 maxXP → 进度条无意义；改为 `LV_THRESH[MAX_LEVEL] * SKILLS.length`。
- **协同线数据/视图不同步**：SYNERGIES 7 条 / SVG 4 条 错位；改为 `SYNERGY_PAIRS` 单一 source，SVG 从派生坐标画。
- **周挑战 XP 全部堆到 spirit-1**：按挑战类型分发到相关技能（cross-3 自动 / streak-3 → 自知 / levelup-1 → 学习 / total-100 → 专注 / brush-5 → 情绪）。
- **importState 拒绝旧版本**：改为走和 loadState 一致的迁移链。

---

## [1.2.7] — 2026-09-07

### 修复

- `renderChangelogModal` 在 mount 元素未渲染时静默失败，不再抛错。
- 从 `renderAll` 中移除 `renderChangelog` 调用（modal 按需渲染）。

---

## [1.2.6] — 2026-09-07

### 设计

- 更新日志独立成 modal（顶栏按钮），不再混在方法 tab 底部。

---

## [1.2.5] — 2026-09-07

### 备注

- `CHANGELOG_ENTRIES` 所有日期占位符 "—" 替换为真实日期。
- git tag 命名从 v3.0.x 切到 v1.2.x 系列（产品代码版本号保持 `changji-state` v1）。

---

## [1.2.4] — 2026-09-06

### 修复

- **同列协同线删除** —— 自知 ↔ 情绪 是同列相邻节点（道脉 r0 → r1），列脊柱已表达递进；删除冗余斜线。
- **协同线只保留跨列近迁移（4 条）**，同列靠脊柱。

## [1.2.3] — 2026-09-06

### 修复

- 自知 ↔ 情绪 两根对角线中一根冗余，删一根。

## [1.2.2] — 2026-09-06

### 修改

- **五脉二十技排版逻辑化** —— 按"基础 → 综合"重排身 / 心 / 技 三脉：
  - 身：柔韧 → 体能 → 力量 → 耐力（关节活动度 → 心肺 → 肌肉 → 综合）
  - 心：记忆 → 专注 → 逻辑 → 创造（存储 → 提取 → 综合 → 重组）
  - 技：学习 → 沟通 → 写作 → 决策（元技能 → 人际 → 单向 → 应用）
- 群 / 道 两脉已合理（感他 → 立我 → 互构 → 群体；内观 → 与感受 → 冲突中 → 长程），不动。
- 协同线 SVG 坐标按新位置调整（5 条跨列连接）。

## [1.2.1] — 2026-09-06

### 修改

- **方法 tab 引用直链原文**：5 个引用链接从 `REFERENCES.md#N` 改为论文原文 URL。
  - Dreyfus 1980 → Open Library `Mind over Machine`
  - Lally 2010 → Wiley（DOI 10.1002/ejsp.674）
  - Ericsson 1993 → APA PsycNet（DOI 10.1037/0033-295X.100.3.363）
  - Salomon & Perkins 1989 → SAGE（DOI 10.3102/0013189X018009042）
  - Flavell 1979 → APA PsycNet（DOI 10.1037/0003-066X.34.10.906）
- 文案从「详见引用 →」改为「论文原文 →」，点击跳转新标签页（target="_blank" rel="noopener"）。
- **REFERENCES.md 同步**：每节加上"原文"行，DOI 字段改为可点击。

## [1.2.0] — 2026-09-06

重大重设计：从 narrative-style 长滚动改为 **底部 5 tab 单页**。

### 修改

- **HTML 结构**：从 9 节长滚动 → 5 个 tab pane（角色 / 树 / 今日 / 日志 / 方法），每次只显示一个。
- **顶部精简到一行**：品牌 + 角色 pill（点击切到角色 tab）+ 日志 / 设置两个图标按钮。
- **底部固定 tab bar**：5 个 tab 均分宽度，激活态有顶部 border + 加粗。
- **URL hash 路由**：`#character` / `#tree` / `#today` / `#log` / `#method`，浏览器前进后退工作。
- **默认 tab = `树`**：daily-use 最常见的探索场景。
- **设置集中化**：导出 / 导入 / 重置 / 关于 README 全部进「设置」弹窗（之前散落在 footer）。
- **更新日志双位置**：方法 tab 底部展示 + topbar「日志」按钮触发完整弹窗。
- **树紧凑化**：legend 横排到标题、tree-wrap 内边距 6px、SVG max-width 860px。

### 新增

- **角色 pill（topbar 中央）**：实时显示 `Lv.X · 第 N 天`，点击切到角色 tab。
- **今日 tab 顶部 summary**：今日已获 XP / 笔触状态 / 周节律三项 inline。
- **日志 tab 顶部统计**：总记录 / 近 7 日 / 升级次数 / 累计 XP 四张卡。
- **fadeIn 动画**：tab 切换时新 pane 0.2s 淡入。

### 设计依据

每次来只做一件事：勾 / 看 / 记 / 回顾。把 mental mode 拆成独立屏，专注度提升，"滚到底才能看到方法"的疲惫消失。

### 文件变化

- 主文件：`real-life-skill-tree.html`（v1.2.0，tabbed）
- 备份：`real-life-skill-tree-v1.1.3-narrative.html`（v1.1.3 长滚动版）
- 数据 schema 不变（仍是 `changji-state` v1）；旧 v1.1.x / v1.0.x 用户数据自动兼容。

---

## [1.1.3] — （日期不明，本次会话前）

### 新增

- **更新日志 tab** —— topnav 加 `<a href="#changelog">更新日志</a>` 链接；页面末尾新增"第九卷 · 版本"章节，渲染 `CHANGELOG_ENTRIES` 数组。
- **结构化展示** —— 每条改动带类型徽章（新增 / 改动 / 修复 / 设计 / 备注），比本文件更易扫读。
- **JS 数据镜像本文件** —— `CHANGELOG_ENTRIES` 数组与本文件语义一致，修改时两边同步。

## [1.1.2] — （日期不明）

13 项技能的修习内容从占位符补到具体条目。原 7 项不动。

### 新增

- **身**：力量（复合动作 / 推拉腿分化 / 自重循环）、柔韧（静态拉伸 / 关节绕环 / 阴瑜伽）、耐力（长距慢跑 / 通勤骑车 / 楼梯）
- **心**：记忆（Anki / 复述要点 / 关键词记名）、逻辑（标前提-结论-假设 / 反驳自己 / 苏格拉底追问）
- **技**：沟通（即兴介绍 / 复述再表态 / 三抽象层级讲同一事）、学习（教别人 / 认知地图 / 间隔复习）、决策（列最大损失 / 10-10-10 / 可逆小实验）
- **群**：边界（拒绝一个请求 / 边界清单 / 练说"我不"）、关系（联系老友 / 主动约见 / 写信）、领导（无议程讨论 / 放手给任务 / 复盘失败）
- **道**：价值（动机复盘 / 不妥协清单 / 冲突暂停 24h）、意义（值得瞬间 / 30 天目标 / 回到核心问题）

### 设计原则（每项 3 条统一遵守）

1. **单次可完成**：最长 90 分钟，最短 5 分钟
2. **执行 + 反思混合**：3 条里通常 2 条执行类 + 1 条反思类
3. **不重叠**：与已有技能条目不重复
4. **无依赖**：不假设有器械 / 伙伴 / 付费工具

### 未来（v2 范围，未做）

- **任务类型换挡**（契克森米哈赖）：同一技能在 Lv.4 时条目侧重"教别人 / 跨场景"，在 Lv.5 侧重"风格化"。

## [1.1.1] — （日期不明）

按圆桌第二轮（Sivers + 7 位共识）落地：废除 streak 显示，改为周节律。

### 修改

- **角色面板"连修"卡片 → "周节律"** —— 不再显示"连续 N 天"。改为显示过去 7 天中有活动的天数（N/7）。
  - 旧："连修 23 天"  + "再 7 天进入自动化区间"
  - 新："周节律 4 / 7 天" + "本周围 4 天，节奏起来了。"
- **`computeStreak` → `computeWeeklyCadence`** —— 算法从"连续天数"改为"过去 7 天有活动的天数"。Loss aversion 移除。
- **同步修复盲点 7** —— 3 天高强度 + 4 天休息的人不再被惩罚为"断了"。他们现在显示"周节律 3 / 7"，与连续 7 天的人同为稳定参与者（一个连续型、一个集中型）。

## [1.1.0] — （日期不明）

v1.1 圆桌讨论落地：从克利尔、契克森米哈赖、Sivers 等 7 位代表的共识中提取的若干补丁。

### 新增

- **「今日一笔」自画字段**（克利尔身份补丁）—— 每天一句话"今天我做了一次 X 会做的事"。
- **定性化挑战显示**（契克森米哈赖 + 克利尔）—— 角色面板和详情页的"下一关"改为文字描述，XP 数字降级。
- **等级挑战表 `LEVEL_CHALLENGES`** —— 五个等级过渡各有挑战描述 + "本周可尝试"具体动作。
- **章节顺序调整** —— 第五卷半"今日一笔"放在"今日修习"之前（先决定"我是谁"，再记录"我做了什么"）。
- **Hero 文案改为"骑士 / 法师 / 猎人"** —— 保留三段式节奏，去掉具体游戏名。

## [1.0.0] — （日期不明）

### 新增

- 20 项技能 × 五脉 SVG 树（身 / 心 / 技 / 群 / 道）。
- 角色面板 / Quests / Featured / 方法 / 日志 / CTA / Footer 七个 section。
- localStorage 持久化（schema 1，带 migration shim）。
- 5 项研究的 REFERENCES 链接 + 7 日反思模板。

### Meta

- 许可证 CC BY-NC 4.0。
